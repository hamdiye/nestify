package com.nestify;

/**
 * Application integration tests.
 * Note: Spring context tests requiring a database are excluded from CI.
 * Use @ExtendWith(MockitoExtension.class) based unit tests instead.
 */
class NestifyApplicationTests {
    // Unit testler için UserServiceTest ve benzeri Mockito tabanlı testleri kullanın.
}
